# Cross-Stage Decision Ledger

A compact index of every significant technical decision, with its current evidential status and **what would cause us to reverse it**.

This ledger is a *navigation aid*. The full argument for each decision lives in `docs/architecture_decisions.md`; it is not duplicated here.

**Status vocabulary** (same as the ADR register):
`ACCEPTED` — validated by experiment · `PROPOSED` — reasoned but unvalidated · `SUPERSEDED` · `REJECTED`

**A decision is not `ACCEPTED` merely because it has been implemented.** Six of the twelve ADRs remain `PROPOSED` despite being fully built and in daily use, because the experiment that would validate them has not run.

---

## Accepted — validated by experiment

| ID | Date | Stage | Decision | Alternatives considered | Evidence | Reason | Status | Would reverse if |
|---|---|---|---|---|---|---|---|---|
| **D-007** (ADR-0007) | 2026-08-24 | PHASE-0 | Build `geometry/` **first**, with property tests, and keep it **independent of OpenCV** | (a) build pipeline end-to-end and fix conventions as bugs surface; (b) adopt OpenCV conventions implicitly | 70 tests; all 5 models recover at ~1e-13 px against a 0.1 px gate; Hartley holds condition ~3.4 across 512 → 60 000 px | Coordinate bugs are silent and corrupt everything downstream (risk R8). Implicit conventions are how they are born | **ACCEPTED** | A measured demonstration that a single external convention source is *less* error-prone than an explicit local one. The E-001 measurement (naive form wrong by 1.0 px at s = 3) makes this unlikely |
| **D-003** (ADR-0003) | 2026-08-24 | EXP-001 → EXP-002 | **Fit RMSE can never stand alone as a correctness signal.** Every RMSE labelled `fit` or `held-out` and quoted with its inlier count; headline claims use the *worst* applicable estimator | (a) report inlier RMSE as the literature does; (b) report only synthetic-GT error; (c) manually annotate tie points | 11 of 17 EXP-001 failures reported fit RMSE < 1e-12 px while 190–2400 px wrong. Pre-declared negative control in EXP-002: **ROC AUC 0.495** vs `n_inliers` 0.993 | In the failure regime fit RMSE is not merely circular but **inverted**, best-valued at total failure | **ACCEPTED** | Nothing plausible. Two independent lines of evidence, one of them a negative control declared in advance |
| **D-009** (ADR-0009) | 2026-08-24 | EXP-001 | Retain RootSIFT as baseline **B1**, but **drop the claim** that it beats plain SIFT; promote neither to the final pipeline | (a) promote RootSIFT on the literature; (b) revert to plain SIFT; (c) drop SIFT-family entirely | Group G: no measurable advantage; the 0.136 px difference at Δaz 0 is inside the ±0.59 px run-to-run variation later measured (RL-016) | Spec §69 forbids repeating an unverified published claim. RootSIFT is free, so retaining it costs nothing | **ACCEPTED** | A properly replicated multi-seed comparison showing a difference larger than run-to-run variance |
| **D-010** (ADR-0010) | 2026-08-24 | EXP-001 | **Promote EXP-003 (representations) ahead of the remaining classical baseline sweep** | (a) finish the baseline sweep first; (b) skip to a learned matcher | Illumination cliff (total failure) vs scale surviving to 4× and Δel −30° surviving, in the same suite | Spec §88: choose the next step from measured weakness, not a preset plan | **ACCEPTED** | Evidence that a different axis dominates on real data |
| **D-011** (ADR-0011) | 2026-08-24 | EXP-002 | **Loop closure is the primary GT-free correctness check.** Held-out residual and spatial split consistency demoted to **degeneracy detection only**; cycle consistency retained with a documented blind spot | (a) combine all four into one confidence score, as ANALYSIS §F.1 implied; (b) keep only held-out residual; (c) drop the stack and rely on inlier count | Loop closure **1.000 detection / 0.000 false alarm**; held-out and split **0.200 detection**, blind to every coherent wrong case (64 px wrong → 0.468 px residual) | Averaging a working detector with two blind ones dilutes the signal and manufactures false confidence | **ACCEPTED** (mathematics only) | Real-data evidence that per-edge errors are correlated in a way that cancels around a loop; or that overlapping triplets are unavailable, making it inapplicable |
| **D-012** (ADR-0012) | 2026-08-24 | EXP-002 | **Terrain realism controlled by an explicit slope target anchored to LOLA statistics**; extreme terrain retained as a diagnostic; conclusions reported **per regime** | (a) keep old terrain with a caveat; (b) replace it outright; (c) tune by eye until it "looks lunar" | A-regimes hit LOLA medians exactly (3.50 / 9.10). EXP-001 terrain: **89.55% above the angle of repose**. Two conclusions changed as a result (E-014, E-015) | "Realistic" is a claim, and spec §70 requires claims checkable against authoritative sources. "Looks lunar" is unfalsifiable | **ACCEPTED** | Real lunar imagery showing the synthetic slope/texture statistics mislead in some other direction |

---

## Proposed — reasoned, implemented, but **not yet validated**

| ID | Date | Stage | Decision | Alternatives considered | Evidence | Reason | Status | Would accept when / reverse if |
|---|---|---|---|---|---|---|---|---|
| **D-001** (ADR-0001) | 2026-08-24 | PHASE-0 | Fixed geometric protocol with a **swappable matcher engine**; do not select the matcher yet | (a) commit to one learned matcher; (b) purely classical; (c) ensemble | SAR-optical benchmark: protocol choices change mean error **up to 33×**, exceeding the top-vs-mid matcher gap (sources.md S4) | If it transfers, effort belongs on the dominant axis — and those stages are CPU-cheap | **PROPOSED** | **Accept** if EXP-006 reproduces the protocol-dominance effect on lunar data. **Reverse** if matcher choice dominates protocol there. This is SAR-optical evidence, not lunar |
| **D-002** (ADR-0002) | 2026-08-24 | PHASE-0 | **CPU-first**; GPU optional, never a runtime dependency of the deliverable | (a) require a GPU; (b) rent cloud GPU; (c) classical only | Measured: no CUDA, 4C/8T, 15.2 GB (RL-001). Published GPU latency: XoFTR 0.4 s vs RoMa 5.0 s/pair | A deliverable requiring hardware we lack is not a deliverable (spec §56) | **PROPOSED** | **Accept** when EXP-005 measures actual CPU latency per engine. Option (c) was rejected for foreclosing engines before measuring them |
| **D-004** (ADR-0004) ~~PROPOSED~~ **SUPERSEDED by EXP-003 (RL-023)** | 2026-08-24 | PHASE-0 | ~~Default matching representation is polarity-agnostic structure~~ — **refuted: mod-π was the worst of six arms (wins 0, loses 38 vs its own 2π control on identical keypoints); raw intensity won** | (a) raw intensity; (b) CLAHE/histogram matching; (c) rely on learned invariance; (d) physical relighting | Structural: SIFT/ORB/AKAZE bin gradient orientation over `[0, 2π)`; contrast normalisation is sign-preserving and cannot repair a sign flip. Measured: image gradients anti-correlate at **−0.853** under azimuth reversal | Prefer invariance provable by construction over invariance merely trained and hoped for. **The invariance was provable and was not enough:** phase congruency is contrast- and polarity-invariant to 1e-6 / exactly 0.0 and still stops at 27–30°, because shadow motion changes *which structures exist*, not their contrast | **SUPERSEDED** (2026-08-24, EXP-003) | **Already reversed.** EXP-003 met the ADR's own supersession condition: raw intensity won. Reinstate only if a polarity-agnostic representation is shown to beat raw intensity on a realistic A-regime under a pre-registered criterion |
| **D-005** (ADR-0005) | 2026-08-24 | PHASE-0 | **Scale normalisation is a required pipeline stage**, not preprocessing | (a) rely on the matcher's scale invariance; (b) multi-scale matching without normalisation; (c) require metadata | Published GSDs give pairwise ratios 2:1 … **320:1** (RL-002, sources.md S1/S2); off-the-shelf matchers are reliable to ~2–4× | Option (a) is refuted by arithmetic. Option (c) is refused by spec §24 (must degrade without metadata) | **PROPOSED** | **Accept** when EXP-004 sweeps the real ladder and shows where normalisation stops rescuing the match. Note EXP-002 already showed scale tolerance is texture-dependent (E-015) |
| **D-006** (ADR-0006) | 2026-08-24 | PHASE-0 | **`max_uncovered_disc_radius`** is the primary coverage metric; occupancy, entropy, hull ratio secondary | (a) grid occupancy; (b) spatial entropy; (c) convex hull ratio | Argument from what the metric *bounds*: registration error grows with distance to the nearest constraint, so the largest empty disc upper-bounds worst-case local error. Averages cannot bound a worst case | Spec §15 asks which metric is most meaningful **and for the justification** | **PROPOSED** | **Accept** if EXP-007 shows it correlates with local held-out error better than the alternatives. **Reverse** if it does not — then it is simply the wrong metric. Supporting anecdote only: it flagged a correct-but-fragile 8-inlier case that error metrics called a success |
| **D-008** (ADR-0008) | 2026-08-24 | PHASE-0 | **Licence audit gates every pretrained weight**; prefer Apache-2.0 / MIT | (a) use best weights, address licensing later; (b) avoid pretrained weights entirely | Risk R7. SuperGlue's weights are *believed* research-/non-commercial-only — **recorded as UNVERIFIED**, to be checked against the actual licence text | A licence problem found at evaluation is unrecoverable; choosing permissively early costs nothing | **PROPOSED** | **Accept** once the audit table exists and is populated. Currently no weights are in use, so nothing is blocked |

---

## Operational decisions recorded outside the ADR register

Smaller decisions with evidence, recorded in stage reports and code rather than as ADRs.

| ID | Date | Stage | Decision | Evidence | Status | Would reverse if |
|---|---|---|---|---|---|---|
| **D-013** | 2026-08-24 | EXP-001 | **Defer learned matchers** until a better *representation* has been tested with classical machinery | Spec §36/§75: a component ships only against a measured deficiency. EXP-005 (CPU latency) and ADR-0008 (licensing) both unresolved | Active | EXP-003 showing no representation closes the gap, making a learned engine the remaining option |
| **D-014** | 2026-08-24 | EXP-001 | Introduce OpenCV **for SIFT/RootSIFT only**; geometry stays OpenCV-free | User-approved scope; `geometry/` has no OpenCV import | Active | Only if geometry gained a dependency, which D-007 forbids |
| **D-015** | 2026-08-24 | EXP-001 | Cap `nfeatures` at **0 (uncapped)** in SIFT | Capping makes OpenCV retain the strongest responses, concentrating keypoints in high-contrast terrain and pre-damaging the spatial coverage spec §15 asks about | Active | If uncapped detection becomes a runtime problem at NAC scale |
| **D-016** | 2026-08-24 | EXP-001 | Set the "correct match" threshold **equal to** the RANSAC threshold (both 3.0 px) | Otherwise `reported_inlier_ratio` and `true_inlier_precision` are judged on different yardsticks and the comparison between them — the point of the metrics module — is not like-for-like | Active | If a use case needs a different correctness tolerance from the inlier tolerance |
| **D-017** | 2026-08-24 | EXP-002 | **`lo_max_points` caps the LO refit sample, never the consensus scoring** | A 1000-point least-squares fit is already vastly over-determined for ≤ 8 DOF; capped-vs-uncapped difference < 0.05 px, and all 3000 true inliers still found with a cap of 50 | Active | If a model class appeared where 1000 points under-determine the refit |
| **D-018** | 2026-08-24 | EXP-002 | **Preserve `results_preEXP002.csv`**; never overwrite superseded results | Integrity rule 3/4. It is what makes the 0/45-flip comparison and the ±0.59 px tolerance demonstrable | Active | Never |
| **D-019** | 2026-08-24 | EXP-002 | **Realistic mare becomes the priority benchmark** for EXP-003 | Fails earliest on both axes (cliff at 15°, scale at 2×) and has **312 vs 22 119 kp/Mpx** | Active | If real lunar imagery shows mare-like terrain is rare in the evaluation set |
| **D-020** | 2026-08-24 | EXP-002 | **Runtime is no longer a primary constraint** | Median pipeline 0.387 s, worst case 3.64 s after the ~968× RANSAC fix, against a ≤ 60 s/pair budget | Active | Reinstated when tiling full NAC frames (10k × 52k) or if a learned engine's CPU latency proves prohibitive (EXP-005) |
| **D-021** | 2026-08-24 | EXP-002 | Report **per regime, never pooled** | Conclusions invert between regimes: scale is easy on highlands, fails at 2× on mare | Active | Never — pooling across regimes is what produced E-014 and E-015 |
| **D-022** | 2026-08-24 | EXP-002 | Override the automated `best_deployable_signal` selection | `split_consistency` scored highest by AUC but was an artefact of an `inf` sentinel (E-010); `n_inliers` reported instead | Active | If a future run shows the estimator finite and discriminative on wrong cases |
| **D-023** | 2026-08-24 | EXP-002 addendum | **The deployable failure rule is `n_inliers <= 8`** (equivalently `n_inliers < 9`), not the previously documented `n_inliers < 8` | Exactly one case in 384 discriminates: a calibration failure with **exactly 8 inliers and 367.95 px** true error. `< 8` misses it (calibration recall **0.9894**); `<= 8` catches it at an **identical** FPR of 0.0612. On validation the forms are indistinguishable — **no validation case has 8 inliers**, so the boundary was never tested. Youden's J had selected `<= 8` (`objective3_ninliers_sensitivity.json:diagnostics.rule_form`) | Active | If a larger validation set populates the boundary and shows `<= 8` costs false alarms that `< 8` avoids |

| **D-024** | 2026-08-24 | EXP-003 | **Record the orientation-*assignment* mechanism as a pre-registration target for EXP-004 — NOT as an accepted decision** | SIFT's assigned dominant orientation drifts ~1:1 with Sun azimuth: median \|Δangle\| 6.69° → 44.77° as Δaz goes 0° → 45°, with 67% of keypoints off by >30° at 45°. The upright control arm, on **identical keypoints** (99/99) and matched coverage (0.129 vs 0.130), extends the cliff 27° → 40° on B-regime | **Held — not accepted** | **Accept** only if EXP-004 pre-registers it and reproduces it. **Reject** if it does not survive pre-registration, or is shown to be an artefact of the custom descriptor. It is post-hoc, single-stage, synthetic, and uprightness is not deployable beyond ~8° relative rotation |
| **D-025** | 2026-08-24 | EXP-003 | **Do not quote `n_inliers <= 8` at FPR 0.0112. EXP-004 must re-fit an operating point on its own calibration seeds** | EXP-003 mixed correct/wrong cells: recall **0.9857** (transfers, ≥ 0.95) but FPR **0.3692** — 33× worse than EXP-002's 0.0112. Per regime: mare 0.4706, highlands 0.3077, B 0.3636 | Active | Never for the 0.0112 figure. A future stage may establish a *new* operating point, on disjoint calibration and validation seeds |
| **D-026** | 2026-08-24 | EXP-003 | **Resolve whether realistic mare at 384² is measurable at all, before any further mare conclusion** | Mare yields **24 keypoints** at 384². The two phase-congruency arms fail the Δaz = 0 positive control there, voiding their mare results. Best mare result across all six arms is the baseline's 21° | Active | Discharged when EXP-004 re-measures the mare cliff at 768²/1024². If mare was capacity-limited, every mare conclusion in EXP-002 and EXP-003 must be re-read |
| **D-027** | 2026-08-24 | EXP-003 | **Label a GT-free statistic with the thing it measures: loop closure is scored loop-level, never edge-level** | Edge-level labelling scored loop closure at false alarm **0.617**; loop-level labelling gives **0.000**, detection 1.000. A 0.617 swing from a labelling choice alone | Active | Never — this is contradiction D2 recurring, and the mislabelled variant is retained in the artefact as `detection_edge_level_MISLABELLED` to keep the size of the error visible |
| **D-028** | 2026-08-24 | EXP-003 | **Learned matchers remain deferred.** EXP-004 is illumination-stable orientation assignment plus the mare measurability question | Pre-registered stop-condition 1 fired: no representation met S1. A learned matcher would inherit the same broken orientation reference and the same possibly-unmeasurable mare benchmark | Active | When EXP-004 has a pre-registered orientation result and a measurable mare benchmark, and a learned engine can be argued against a *measured* deficiency (spec §36/§75) |
| **D-029** | 2026-08-25 | REAL-DATA | **Pair selection needs an incidence CEILING, not only a difference floor.** Both incidences must be bounded (≤ 75° was used here) before Δincidence is maximised | The unbounded selector's top pick was 89.96° vs 24.64°. The 89.96° frame decodes correctly and is unusable: DN median **29 ± 19.6**, lag-1 autocorrelation **0.3553** vs 0.9759 for its partner, 2 515 keypoints vs 9 706, **5 putative matches** (E-026, `tile_sanity_terminator.json`). A ≤ 75° survey returned **32 usable pairs** | **Active — recorded, NOT yet implemented in `find_illumination_pairs()`** | If a method is demonstrated that recovers usable structure from terminator imagery, or if a stage shows the ceiling is discarding pairs that would have registered. The specific value 75° is a **provisional cut, not a validated threshold** — no sweep was run |
| **D-030** | 2026-08-25 | REAL-DATA | **Real-data tile overlap is selected APPROXIMATELY and must be labelled "pre-registration overlap selection" wherever it appears.** Line index is assumed linear in latitude from the ODE footprint polygon; cross-track position is the frame centre | No camera model and no SPICE kernel is available to this project, so no exact pixel ↔ (lat, lon) mapping exists. Supporting consistency check: implied m/line from the footprint agrees with ODE's independent `Map_resolution` at a ratio of **1.050 / 1.050 / 1.044** across three frames from different orbits and years. That supports a *first-order* along-track model and nothing more — a constant 5% bias is exactly what such a model would show | **DISCHARGED 2026-08-26 by REAL-DATA-02**, by a route not listed here: the PDS archive index table names the frame corners, so tile ground footprints are computed directly. The approximation this decision described is **superseded, and it was wrong in a way the consistency check could not see** -- the 1.050 ratio was a comparison against `Map_resolution`, which is not the down-scan pixel scale; against `SCALED_PIXEL_HEIGHT` the ratios are 1.002-1.012. The original text above is retained under integrity rule 3 | Discharged when a camera model, SPICE, or an image-content overlap search establishes overlap independently of the matcher. Until then **no real crop may be described as geographically registered** -- and that restriction still stands: REAL-DATA-02 establishes shared *ground*, not a known transform |
| **D-031** | 2026-08-25 | REAL-DATA | **The first real registration is classified C (registration failure) and no accuracy figure is quoted from it.** The cause is recorded as UNRESOLVED between the overlap and illumination hypotheses | 3 inliers / 35 putative, fit RMSE **1.575e-12 px**, transform with 11–18× scale and ~10 000 px translation, verdict REJECTED (`registration_usable.json`). All four H1/H2 combinations fail (3, 4, 4, 5 inliers), so the line-direction ambiguity is **not** the explanation (`hypothesis_grid.json`). Δ*azimuth* is unavailable (E-020), so EXP-003's cliff is not directly applicable either | **Active, and REAL-DATA-02 changes which run it applies to.** The 3-inlier run it was written about (`registration_usable.json`) used tiles **22.75 km apart** and is now known to measure nothing about the matcher (E-028). The decision transfers to `registration_usableH2.json` -- 5 inliers on tiles independently confirmed to share **68.6 % / 70.5 %** of their ground -- where overlap is now excluded as the cause and the failure remains **unattributed** | Attribution becomes possible only once overlap is established independently (D-030, now discharged for `usable_H2`). **Never** by assertion: the failure must still not be attributed to illumination, because mare texture poverty (D-026), decimation, resolution ratio and real relief displacement are all untested. **SUPERSEDED 2026-08-26 by D-036**: REAL-DATA-03 tested exactly those alternatives against a succeeding edge on the same ground and eliminated all of them, leaving illumination as the only surviving candidate -- still not the established cause, because it is confounded with frame identity across three frames |
| **D-032** | 2026-08-25 | REAL-DATA | **A decoded tile must pass intrinsic sanity checks before it may enter the pipeline**, and a failure must name whether the cause is the *decode* or the *data* | Each failure mode produces a finite, correctly-shaped, wrong array: byte-order reversal, one-byte offset shift, truncation, sentinel contamination. Positive controls bound the interpretation — self-registration recovers identity to **1.14e-12**, and a known (+40, +25) px shift on real imagery is recovered as **(40.142, 24.941)**, error 0.14 px | Active | Never for the principle. The specific `MIN_PLAUSIBLE_AUTOCORR = 0.60` floor and `DECODE_EVIDENCE_MARGIN = 0.10` are **provisional constants chosen from four tiles**, and either may be re-fitted once more real products exist |
| **D-033** | 2026-08-26 | REAL-DATA-02 | **Real tile windows must be derived from archive corner geometry, per frame, on BOTH axes.** The line-direction hypothesis is a property of the individual frame, not of the camera or the pair, and the cross-track window must be placed on a shared ground point rather than at each frame's own centre | Applying one global hypothesis put the `usable` pair's tiles **22.75 km apart** (intersection **0.0000 km2**), and that pair produced REAL-DATA-01's headline 3-inlier result (E-028). Taking each frame's centre samples cost a further ~1.1-1.7 km of cross-track offset against a 1.8 km tile, capping `usable_H2` at **68.6 %** and `terminator_H1` at **28.3 %** (E-029). The archive states the direction: `LRO_FLIGHT_DIRECTION` **-X** -> line 0 at MIN latitude, **+X** -> MAX, matching the corner latitudes 4/4. Geometry-driven windows project to **97.1 %** (usable) and **82.5 %** (terminator) -- `experiments/REAL-DATA-02/overlap_verification.json` | **IMPLEMENTED AND MEASURED 2026-08-26 by REAL-DATA-03.** `acquire_real_pair.py --from-geometry` derives the window per frame on both axes; the old path is retained unchanged so REAL-DATA-01 stays reproducible. Measured outcome: tile-centre separation **1 metre**, overlap **97.12 %** against the 97.12 % projection, by an independent code path. The projection is no longer a projection | If a product is found whose PDS4 `disp:Display_Direction` does not read `(Line, Top to Bottom)`, or if a camera model / SPICE reconstruction contradicts the index-table corner naming. The **97.1 %** and **82.5 %** figures are **projections from geometry, not measurements** -- no such tile has been cut |
| **D-034** | 2026-08-26 | REAL-DATA-02 | **Tile overlap is classified from an uncertainty INTERVAL against fixed criteria, and UNKNOWN is a permitted answer.** CONFIRMED requires the pessimistic (p5) shared fraction of the worse-covered tile >= 0.50; INSUFFICIENT requires the optimistic (p95) < 0.20; anything straddling either threshold is UNKNOWN | The criteria are anchored to the only regime in which this project has *measured* its matcher: `scripts/exp002_common.py` holds source-to-target overlap at **0.757 at worst, 0.945 median** over 5 transform classes x 20 draws. 0.50 sits below that floor, so CONFIRMED does not claim equivalent difficulty. Uncertainty is 4000 Monte Carlo draws (seed 20260826) over the +/-0.005 deg corner quantisation. Applied to five tile pairs it returned 1 CONFIRMED, 3 INSUFFICIENT and **1 UNKNOWN** (`terminator_H1`, p5-p95 **0.234-0.330**) | Active | If a stage measures where a real matcher's success probability actually falls off with overlap fraction, which would replace both thresholds with fitted ones. The values are **provisional cuts reasoned from the synthetic regime, not measured thresholds** -- no sweep was run |
| **D-035** | 2026-08-26 | REAL-DATA-03 | **A real registration result may not be interpreted until independent overlap has been CONFIRMED, and the gate is enforced in code, not by discipline.** `scripts/verify_tile_overlap.py --require-confirmed` exits non-zero unless every edge is OVERLAP_CONFIRMED, and is run as a separate command before the baseline | REAL-DATA-01's headline result was a 3-inlier failure on tiles later shown to be **22.75 km apart** (E-028), and the stage could not tell. Here the gate passed first -- A/B 97.12 %, A/C 82.72 %, B/C 85.00 %, all p5 above the 0.50 criterion -- and only then did the baseline run. `experiments/REAL-DATA-03/overlap_usable_geo.json`, `overlap_triplet.json` | Active | Never for the principle. The specific mechanism would change if an independent geolocation better than the archive's 0.01 deg corners became available (a camera model or SPICE), which would raise the gate's resolution rather than remove it |
| **D-036** | 2026-08-26 | REAL-DATA-03 | **The real-data failure is NOT attributed to illumination.** Illumination difference is recorded as the only enumerated candidate the evidence has not eliminated, and explicitly not as the cause | Three edges on the same mare ground, same instrument, decoder, preprocessing, 2x decimation, affine model, RANSAC threshold and seed, all near-nadir, all overlap-CONFIRMED >= 82.7 %: **B->C (dInc 0.96 deg) gives 5365 inliers; A->B (39.81 deg) and C->A (38.85 deg) give 4 each**. That eliminates overlap, mare texture poverty, decimation, resolution mismatch, relief displacement, window uncertainty, the affine model and any pipeline defect (REAL-DATA-03 section 15.3). **But in a three-frame design, large dIncidence is perfectly confounded with 'the pairing involves frame A'** -- A is the only low-incidence frame and is in both failing edges -- and dIncidence is separately confounded with dAzimuth, which was not measured (RL-032b) | Active | **REAL-DATA-04** decides it with one more frame D at low incidence: if D<->A succeeds and D<->B fails, illumination is the driver; if the reverse, frame identity is, and the surviving candidate is refuted. The prediction is recorded before the data exists |
| **D-037** | 2026-08-26 | REAL-DATA-04 | **Frame D's incidence band is a HARD FILTER at [9.95 deg, 39.95 deg], asymmetric about frame A's 29.95 deg, and it is the stage's independent variable rather than its deciding criterion** | The band is set by arithmetic, before any data: at its upper edge dInc(D<->A) = 10.00 and dInc(D<->B) = 29.81; at its lower edge 20.00 and 59.81. So **dInc(D<->A) <= 20 and dInc(D<->B) >= 29.81 hold everywhere in the band**, and the contrast between them never falls below 19.81 deg -- the design cannot invert. The upper edge is +10 and the lower -20 for exactly that reason. Illumination is a filter and not the decider because the experiment does not exist without a low-incidence D; **resolution ratio stays the decider** (REAL-DATA-03 criterion 3) so that, among frames all admissible by design, the specific frame is still not chosen on illumination | Active | **Discharged by the result.** The selected frame sits at 18.22 deg: dInc(D<->A) 11.73, dInc(D<->B) 51.54, contrast 39.81 -- comfortably inside the band's guarantees |
| **D-038** | 2026-08-26 | REAL-DATA-04 | **A candidate frame whose orientation signature differs from the incumbents' is rejected outright, not weighed** (hard filter 1c). The signature is `(along-track, cross-track)`, computed from the archive's NAMED corner columns | A frame with the opposite signature images the same ground 180 deg rotated, so its tile is rotated 180 deg relative to A's and B's. RootSIFT is rotation invariant *by design*, but whether its orientation assignment holds is **this project's own open question** -- D-024, EXP-004, pre-registered and not started. A failure on such an edge could be illumination, frame identity, **or** orientation assignment, and REAL-DATA-04 exists to separate causes, so it may not introduce a third one into the edge that decides it | Active | **The filter cost the stage its top-ranked candidate** (`nac.m1343417565rc`, the best resolution ratio at 1.1275) and left exactly one admissible frame. Reversal would need EXP-004 to measure that orientation assignment is *not* a failure mode at 180 deg on real tiles -- at which point the filter becomes unnecessary rather than wrong |
| **D-039** | 2026-08-26 | REAL-DATA-04 | **A shared ground point for a tile set is the centroid of the frames' TILE-ADMISSIBLE footprints, not of their frame footprints** (`shared_tile_target()`; `--target-shared-tile`) | D-033's shared-frame centroid answers *do the frames overlap here*, which is the wrong question when the experiment needs a whole **tile** on shared ground. A point inside every footprint but within half a tile of an edge clamps the window, and a clamped tile is no longer on the ground point the manifest records -- REAL-DATA-03 already carried `window_detail.clamped_*` for exactly this. The admissible region is the frame inset by half a tile on each axis | Active | `[MEASURED]` All three REAL-DATA-04 windows are unclamped on both axes (`fully_inside_frame: true`), and the property is covered by 10 tests in `tests/test_ingest_footprint.py`, including a case where the footprints intersect but the overlap is thinner than a tile and the old rule would have returned a point happily |
| **D-040** | 2026-08-26 | REAL-DATA-04 | **The real-data registration failure IS attributed to illumination difference -- specifically to dINCIDENCE -- with its scope stated. Supersedes D-036** | **Six real edges, five frames, two ground windows, two stages, one unmodified pipeline.** Successes at dInc **0.96 deg** (5365 inliers) and **11.73 deg** (1656 inliers, ratio 0.9414, coverage occupancy 1.000); failures at **38.85, 39.81, 39.81 and 51.54 deg** (4, 4, 7, 3 inliers). **dIncidence separates all six**, with a gap between 11.73 and 38.85. D-036's blocking objection -- that dIncidence was perfectly confounded with *the pairing involves frame A* -- **is discharged**: every frame now appears in both a succeeding and a failing edge, so no frame's presence predicts the outcome. The two decisive edges are **overlap-matched to 0.95 pp** (71.30 % vs 70.35 %, CONFIRMED before any registration ran) and differ by a factor of 552 in inliers. The succeeding edge's recovered scale matches SPICE-derived `SCALED_PIXEL` to **0.04 % / 0.13 %** and is the only edge in either stage whose corner-polygon disagreement falls **below** the discrimination floor (0.53x). Overlap, texture, decimation, resolution ratio, demission, acquisition interval, window clamping, orientation, the affine model and a pipeline defect are each eliminated by measurement across both stages (REAL-DATA-04 section 11.1) | Active | **SCOPE, which is part of the decision.** This is d*INCIDENCE*, on mare terrain, in one region, with one instrument, over five frames and two windows. It is **NOT an azimuth result** -- `SUB_SOLAR_AZIMUTH`'s frame is still unverified (RL-032b) -- and it does **not** locate the cliff, which is bracketed only to 11.73-38.85 deg. Reversal: a real high-dIncidence edge that registers, or a real low-dIncidence edge that fails, on the same terrain and pipeline |
| **D-041** | 2026-08-26 | REAL-DATA-05 | **Frame E's incidence band is a HARD FILTER at [18.22 deg, 30.91 deg]** | Arithmetic on the two bracket edges REAL-DATA-04 left behind, fixed before any screening: requiring dInc(E<->A) <= **11.73** (the largest dInc yet observed to SUCCEED) gives inc_E in [18.22, 41.68]; requiring dInc(E<->B) >= **38.85** (the smallest yet observed to FAIL) gives inc_E <= 30.91. The intersection guarantees **both** decisive edges land in a regime whose outcome has already been measured, and that nothing can fall in the unbracketed 11.73-38.85 gap. The contrast never drops below 37.89 deg, so the design cannot invert. Deliberately narrower than D-037's band: it buys a clean replication instead of a cliff-locating datum | Active | **Never exercised.** The screen returned zero admissible frames before the band could decide anything. Locating the cliff is a different experiment and is not attempted under this decision |
| **D-042** | 2026-08-26 | REAL-DATA-05 | **A candidate frame with `EMISSION_ANGLE` > 5.0 deg is rejected outright** (hard filter H6) | The incumbents span 1.17-1.75 deg. REAL-DATA-04 eliminated dEmission empirically, but over a range of **0.58 deg**, which is no licence to admit a frame at 20 deg: an off-nadir E would put relief displacement / parallax on the decisive edge as a fresh variable. The filter only ever **tightens** the candidate set and can never rescue a preferred frame | Active | `[MEASURED]` **It rejected zero candidates.** Both frames that survived every other filter were near-nadir (1.71, 1.72 deg). The stage's outcome is identical with or without it |
| **D-043** | 2026-08-26 | REAL-DATA-05 | **Frame E must be the same NAC camera as A and D (`NAC_FRAME_ID = LEFT`)** (hard filter H7) | Precaution, not necessity, and recorded as such: the six measured edges show camera pairing does **not** partition outcomes (L-L both succeeds and fails; R-L both succeeds and fails). It keeps the **decisive E<->A edge** free of a camera change at the cost of a smaller pool. Derived from the archive product-id suffix and cross-checked against the index table's `NAC_FRAME_ID`, with a disagreement rejecting the candidate rather than picking a winner | Active | `[MEASURED]` **It rejected zero candidates.** Both survivors were LEFT. Reversal costs nothing: dropping it would not have produced an admissible frame |
| **D-040-N1** | 2026-08-26 | **REAL-DATA-05** | **SUPERSEDING NOTE ON D-040 -- a wording correction, not a change to any measurement.** D-040's claim that **frame identity is refuted** is **too strong and is withdrawn**. The defensible statement is: **frame identity is substantially weakened as an explanation, but NOT conclusively refuted.** Every number D-040 cites stands unchanged, and dIncidence remains the attributed cause in the scope D-040 states | Two structural limits, neither a matter of emphasis. **(1) The refutation is a cross-stage join, not a within-experiment result.** Inside REAL-DATA-04's own triplet {A, B, D}, frame **B** sits in both failing edges and no succeeding one -- precisely the partition that blocked REAL-DATA-03, mirrored from A onto B. Only REAL-DATA-03's B->C success breaks it, at a **different ground window**. **(2) n = 1.** Frames A and D each have exactly **one** observation in the successful regime, and it is the **same** observation -- the single D->A edge carries the whole claim for both. **REAL-DATA-05 was the pre-registered replication and returned UNRESOLVED, BY DATA AVAILABILITY**: of 906 archive products only 2 sit in the required incidence band over shared ground, and both are orientation-incompatible with the incumbents, so the screen returned zero admissible frames and the stage stopped rather than relax a criterion. No image byte was fetched and no registration was run, so **nothing in this note is a new measurement** | Active | **Follows the E-032 discipline: the claim is corrected here, in the ledger, and REAL-DATA-04's own report is left exactly as written.** Its section 11.3 heading and Q5 keep their original wording under integrity rule 3. What discharges this note is a **replication** -- a second, independently selected low-incidence frame that passes against A and fails against B. **EXP-004 is now the measured blocker on obtaining one**, not merely an open question: the only two candidate frames were unusable for exactly the reason EXP-004 exists to study |
| **D-044** | 2026-09-04 | EXP-010 | **Local ECC refinement (translation-only, 48 px window) is the deployed sub-pixel step; phase correlation is not.** Refinement runs after the global estimate and before the verdict | `[MEASURED]` ECC 48: 0.0032 px median (95 % CI 0.0032-0.0033) on real NAC self-warps of all four recorded tiles, gate 7e-15 px; phase correlation 0.028 px, recovering about half a residual's magnitude. Under synthetic Sun-azimuth change to 30 deg the refined error stays < 0.25 px on both regimes. No point was made worse by > 0.5 px (`experiments/EXP-010/exp010_results_v2.json`) | Active | A real cross-illumination pair with check points shows the refiner's shading bias exceeding 0.5 px inside the matching envelope (EXP-010 H2 measured only to 30 deg, synthetic) |
| **D-045** | 2026-09-04 | EXP-011 | **The transform model is chosen by held-out evidence on REFINED points (rule B: simplest model within 10 % of the best held-out median, absolute tie floor 0.005 px), and the transform is re-estimated from those points.** Pipeline order: estimate -> refine -> re-estimate with selection -> verify. The verdict records `model_selected_by` | `[MEASURED]` 48/48 correct models, 0.0018 px dense median (CI 0.0013-0.0033) against 0.0975 px for the affine default (`experiments/EXP-011/exp011_results.json`). Caveat recorded in Part 2: on frame D the held-out residuals of translation and affine are indistinguishable (0.0338 vs 0.0336 px), so the rule succeeds there through its simplicity tie-break, not through separation | Active | A real pair where the true motion needs the general model and rule B's tie-break picks the simpler one by > 0.1 px dense error; or a case where a wrong model's held-out residual is > 10 % better than the true model's |
| **D-046** | 2026-09-04 | EXP-007 | **H0 (DEM-render conditioning) is demoted from central to CONDITIONAL on a DEM finer than the matcher's working GSD.** The SLDEM2015 render tier is retired on Mare Serenitatis at every rung from 1.8 m to 30 m | `[MEASURED]` `dem_render_b1` passes 0 of 4 failing pairs at every rung (S1 NOT MET, S2 MET); the render side yields 0-19 SIFT keypoints on the high-Sun frames and 57-176 on the low-Sun frames against 560-20700 on the images; every leg returns 0-7 inliers; the arm also fails both succeeding pairs at every rung (S6 NOT MET for it). Cause: the 59 m height field contains none of the 10-100 m relief the images are made of (`experiments/EXP-007/exp007_results.json`, `logs/render_check_tier1_rd04.png`) | Active | A NAC-DTM site (SERENRIDGE1, 5 m posts) where the render arm passes a pair the raw arm fails; that would confirm the resolution reading and restore H0 within a stated DEM/GSD ratio |
| **D-047** | 2026-09-04 | EXP-007 | **B4L (DISK + LightGlue, Apache-2.0, CPU) is admitted as an engine arm of the architecture and is the illumination engine at the fine and mid rungs on mare.** Supersedes D-028's deferral of learned matchers. SuperPoint/SuperGlue remain excluded (ADR-0008) | `[MEASURED]` S3 MET: C -> A at 38.85 deg, 56 inliers, CONSISTENT with archive geometry (47.4 px against a 104.1 px floor) where RootSIFT gives 4; at 7, 15 and 30 m the engine registers three of the four failing ~40 deg pairs with 447-2042 geometry-consistent inliers, and does not cross 51.54 deg (0, 5, 37 inliers). It breaks neither succeeding pair at any rung. Determinism shown only on a failing edge (3, 3) | Active | REAL-DATA-07 shows a wrong-pass rate above the verdict's tolerance, or an envelope no wider than B1's (S3 there); or a licence finding against the DISK/LightGlue weights (ADR-0008) |
| **D-048** | 2026-09-04 | REAL-DATA-06 | **REAL-DATA-06 is CLOSED as a null result by construction, and per-frame photometric normalisation is retired as an arm** (E-035). The photometric question moves to per-pixel correction at fine-DEM sites only | `[MEASURED]` Both frozen arms identical to `none` on all six edges; the exploratory per-pixel arm from the 59 m DEM changes counts (5365 -> 2469, 1656 -> 1197) and converts no failing edge at any of five rungs | Active | A per-pixel correction from a DEM at or below the working GSD converts a failing real edge while leaving the succeeding ones intact |
| **D-049** | 2026-09-05 | REAL-DATA-07 | **The illumination attribution (D-040) is RE-SCOPED to frames below ~66° incidence with unclamped tiles, and the replication debt of D-040-N1 stands.** Δincidence is necessary, not sufficient: four of fourteen frames fail against every partner irrespective of Δincidence. The D-029 ceiling (75°, provisional) becomes a stage of its own; frame E2 is investigated before any further replication attempt | `[MEASURED]` 42 geometry-confirmed pairs, B1 north-up: pooled separation p = **0.0042** (RD-03 0.0040, RD-04 0.121); no success above 40° (0 / 4); 0 wrong passes in 37. Replication candidate E2 (`nac.m1315225542lc`, 21.13°) fails vs A (6 inliers, INCONSISTENT) and vs B (3) and against every other partner; `m1199981485rc` (66.9°, clamped 717 samples), `m1096350825rc` (72.3°) and `m1142297886lc` (74.7°) — the three darkest tiles — fail 1 / 16 between them, including at Δinc 5–8° (`experiments/REAL-DATA-07/real_data_07_results.json`) | Active | A cause for E2 that is a data defect (placement, orientation, saturation) would restore the unrestricted scope; a 60–75° sweep showing the dark frames succeed against each other at small Δ would move the ceiling below 75° and make the scope a measured one |
| **D-047-N1** | 2026-09-05 | REAL-DATA-07 | **AMENDMENT to D-047.** The learned engine (B4L) is admitted for the coarser rungs (7–30 m, EXP-007 tier 2) and as a **yield** engine inside the classical envelope at native scale; the claim that it extends the native-scale illumination envelope is **withdrawn** | `[MEASURED]` S3 NOT MET: both engines' largest ≥ 0.8 bin is 10–15°; B4L succeeds on 17 / 42 pairs against B1's 20 / 42 and fails every pair B1 fails; where both succeed at Δinc 12–34° its inlier yield is 5–25× B1's. EXP-007's single tier-1 conversion (C → A raw, 56 inliers) gives **0** inliers when the same pair is run A → C north-up | Active | A native-scale pair above 40° that B4L registers geometry-consistently in both directions and both orientations |
| **D-050** | 2026-09-05 | REAL-DATA-08 | **The multimodal claim is NOT supported by the radar proxy, and the 100 m rung IS supported by the learned engine with its geometry verdict attached.** No NAC ↔ Mini-RF pair registers under any engine; NAC ↔ WAC at 100 m registers under B4L on three of four frames and under B1 on one. A pass at a coarse rung is never reported without its geometry verdict | `[MEASURED]` Radar: B1 0 / 16, B7 0 / 16, B4L 0 / 16 (best: 8 inliers, 193 px off). WAC: B1 1 / 4 frames (D, 9 inliers, CONSISTENT); B4L 34–99 consistent inliers on A, B, D from 111 × 46 px NAC strips at ≈ 2 px against a ≈ 2.3 px floor, C INCONCLUSIVE, and **one wrong pass** (B at 59 m, 9 inliers, 28 px off) — the engine's first, 1 / 23 across REAL-DATA-07/08 (`experiments/REAL-DATA-08/real_data_08_results.json`). Box-average degradation, not the PSF-aware operator Part 1 named (deviation recorded) | Active | A radar pair registered geometry-consistently by any engine (a cross-modality arm such as MatchAnything-ELoFTR, licence permitting, is the named candidate); or a wrong-pass rate at the coarse rung above the verdict's tolerance once measured on more frames |
| **D-049-N1** | 2026-09-05 | REAL-DATA-07 (amendment) | **SUPERSEDING NOTE ON D-049.** The four frames that "fail against every partner irrespective of Δincidence" are, for at least E2 and `m1199981485rc`, a **data-orientation defect (E-037: mirrored tiles)**, not a property of the Moon. D-049's re-scoping to *frames below ~66° with unclamped tiles* is **withdrawn pending the amended run**; the incidence-ceiling question stays open for the two dark frames whose handedness matches the incumbents' (`m1096350825rc`) or does not (`m1142297886lc`) | `[MEASURED]` E2 flipped: 5 → 2691 inliers vs D, 6 → 2138 vs A. Determinant sign splits the census 9 / 5, and the 5 are the failing frames plus two that only ever met each other | Active — resolved by `real_data_07_results_nue.json` | The amended run still shows a frame failing against every partner after the flip |
| **D-049-N2** | 2026-09-06 | REAL-DATA-07 (amended run) | **D-049 is WITHDRAWN.** With the orientation step corrected (E-037), the replication criterion S1 is MET and the per-window significance S5 is MET; Δincidence's scope returns to D-040's statement for all frames below ~70°. The only residual frame-level failures are the two darkest frames at 72–75°, which is D-029's ceiling question (RL-042c), not a new cause | `[MEASURED]` `real_data_07_results_nue.json`: E2 → A 2138 CONSISTENT, E2 → B 4 INCONSISTENT; pooled p = 0.0012 (RD-03 0.0043, RD-04 0.029); 0 wrong passes in 76 (25 + 24 + 27); five B1 outcomes changed, all fail → success, all on mirrored frames | Active | A third low-incidence frame that fails against A after the correction |
| **D-040-N2** | 2026-09-06 | REAL-DATA-07 (amended run) | **SUPERSEDING NOTE ON D-040-N1: the replication debt is DISCHARGED.** A second, independently acquired low-incidence frame (E2, `nac.m1315225542lc`, 21.13°) succeeds against A (Δinc 8.82°, 2138 inliers, geometry-consistent, three engines agree) and fails against B (Δinc 48.63°, 4 inliers). Frames A and D each now have n = 2 in the successful regime and the join is within one stage. Frame identity is **refuted within the tested set**, in the scope D-040 states | `[MEASURED]` as above; the first attempt (Part 2) failed only because E2's tile was a mirror image (E-037) | Active | — |
| **D-051** | 2026-09-06 | REAL-DATA-07 (amended run) | **The engine-agreement floor is 3 px, measured on three engines**, and B4X (XFeat) is admitted as the third engine column for agreement | `[MEASURED]` 24 all-success triples: max pairwise disagreement 2.16 px (B1 vs B4X); any geometry-inconsistent transform: min 247 px; B4X alone 27 / 42 successes, 0 wrong passes | Active | A geometry-consistent all-success triple disagreeing by > 3 px, or an inconsistent one under 30 px |
---

## Reversal watchlist

Decisions whose reversal is *expected to be possible* on current evidence, and the experiment that decides each:

| Decision | Decided by | Reversal is plausible because |
|---|---|---|
| ~~D-004 — polarity-agnostic representation~~ | ~~EXP-003~~ | **RESOLVED — SUPERSEDED.** EXP-003 refuted it; ADR-0004 accepted its own supersession |
| D-024 — orientation *assignment* is the dominant failure | **EXP-004** | Post-hoc from a control arm, one stage, synthetic, confounded with the custom descriptor. Held, not accepted. **REAL-DATA-04 deliberately excluded it as a variable** (D-038) rather than testing it, so nothing here bears on it either way |
| ~~D-028 — learned matchers deferred~~ | ~~EXP-004~~ | **RESOLVED — SUPERSEDED by D-047.** EXP-007 measured a licensable learned engine against a real deficiency (four failing ~40° edges) and it converted one at native scale and three at 7–30 m; the deferral's condition — *argued against a measured deficiency* — was met by measurement |
| D-001 — protocol dominates matcher choice | **EXP-006** | The 33× evidence is SAR-optical; lunar transfer is unproven, and this underwrites the project's headline thesis |
| D-006 — `max_uncovered_disc_radius` primary | **EXP-007** | Justified by argument, not correlation with local error |
| D-011 — loop closure primary | real overlapping triplets | Validated only on constructed transforms; real-data frequency unknown. **REAL-DATA-01 did not test it**: a third overlapping real product was not acquired, so loop closure is absent from every real-data result so far |
| D-011 — loop closure's *power* on real data | **a later stage** | **NOT discharged by REAL-DATA-04.** It ran a second time on real data (943.75 px) and again did not manufacture a small residual with two broken legs, so ADR-0011's reversal condition is still not triggered — but this triplet has **one** successful edge. Measuring its discriminating power needs **three**, i.e. three mutually low-Δincidence real frames |
| D-029 — incidence ceiling at 75° | a stage that sweeps the ceiling | The value is a provisional cut from one region's survey, not a measured threshold. **REAL-DATA-07 makes the sweep urgent:** the three frames at 66.9–74.7° fail against every partner (1 / 16), including at Δinc 5–8° (D-049) |
| ~~D-030 — approximate overlap selection~~ | ~~REAL-DATA-02~~ | **RESOLVED — DISCHARGED.** The archive index table's named corners replaced the approximation outright, by a route the decision did not anticipate |
| ~~D-031 — real failure cause unattributed~~ | ~~REAL-DATA-03~~ | **SUPERSEDED by D-036.** A real pair that can succeed now exists; seven alternatives are eliminated by measurement and one candidate survives, unattributed |
| ~~D-033 — geometry-driven tile windows~~ | ~~REAL-DATA-03~~ | **RESOLVED — IMPLEMENTED AND MEASURED.** Tiles cut, overlap measured at 97.12 %, tile centres 1 m apart |
| ~~D-036 — the real failure is not attributed to illumination~~ | ~~REAL-DATA-04~~ | **RESOLVED — SUPERSEDED by D-040.** The confound is broken: frame D at 18.22° registers against frame A (1656 inliers) and fails against frame B (3), on overlap-matched edges. Across six real edges every frame appears on both sides and Δincidence separates all six. **Illumination is now the attributed cause, in the scope D-040 states** |
| D-035 — overlap gate before interpretation | — | Never for the principle; the mechanism improves if a camera model or SPICE becomes available |
| D-034 — overlap criteria 0.50 / 0.20 | a stage that sweeps overlap fraction | Provisional cuts reasoned from the synthetic regime, never measured against real matcher success |
| D-005 — scale normalisation as a stage | **EXP-004** | Direction is near-certain from arithmetic; the *sufficiency* of the stage at 20× and 320× is not |
