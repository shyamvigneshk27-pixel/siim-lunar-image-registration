# Cross-Stage Error Ledger

Every significant failure discovered so far, across all stages. **Nothing here is removed once entered** — a fixed error stays in the ledger with status `FIXED`, because the lesson outlives the bug.

Only failures **documented by repository evidence** are listed. No entries are inferred or invented.

**Severity:** `CRITICAL` would have invalidated published results · `HIGH` corrupts numbers or conclusions · `MEDIUM` affects performance or scope of a claim · `LOW` local/contained.

**A note on what dominates this table.** Of the entries below, **9 are failures of *evidence* rather than of code** — metrics that lied, thresholds validated circularly, conclusions over-generalised from one terrain. Only 5 are ordinary software defects. That ratio is itself the project's main empirical finding.

*(That sentence was written when the table ended at E-021, and its arithmetic was already flagged as discrepancy **D3** in [`README.md`](README.md#discrepancies-found-during-re-verification). It is left exactly as written — integrity rule 3 — rather than restated each time the table grows. **E-022 … E-026 were appended on 2026-08-25** from the REAL-DATA stage; of those five, two are ordinary implementation bugs (E-022, E-025), two are failures of evidence in new measurement code (E-023, E-024), and one is a genuine negative result about a selection criterion (E-026). The qualitative point — evidence failures are at least as common as code defects — survives the addition. **E-027 … E-029 were appended on 2026-08-26** from REAL-DATA-02; all three are **design mistakes, not code defects**, and the 260-test suite in place at the time was passing throughout and could not have caught any of them. **E-030 and E-031 were appended on 2026-08-26** from REAL-DATA-03; both are ordinary implementation bugs in existing code, and both were caught by this stage's data before they reached a reported result.)*

---

| ID | Stage | Problem | Severity | Root cause | Detection | Fix | Validation | Status | Lesson |
|---|---|---|---|---|---|---|---|---|---|
| **E-001** | PHASE-0 | Naive resampling coordinate update `p' = s·p` is wrong by `(s−1)/2` px — **1.0 px at s = 3** | **CRITICAL** | Scaling performed in pixel-index space instead of pixel-edge space, ignoring the pixel-centre convention (C3) | Compared both candidate formulas against **actual image resampling** of a Gaussian at a known sub-pixel position, rather than checking a formula against itself | Contract C4; `scale_transform(s)` returns the correct update **as a `Transform`**, so the factor lives in the transform chain | 0.0000 px vs 0.2500–1.0000 px across 4 scales (`EXP-000/metrics.json`) | **FIXED** | Verify a convention against the physical operation it describes, not against its own restatement. Scale normalisation needs factors to 320× — this alone would have consumed the sub-pixel budget invisibly. |
| **E-002** | PHASE-0 | DLT conditioning degrades at large coordinate magnitude | **HIGH** | Uncentred, unscaled image coordinates make the design matrix ill-conditioned | Swept coordinate extent 512 → 10 000 → 60 000 px recording condition number, instead of testing at one convenient scale | Hartley normalisation in every estimator (C9); condition number returned in `EstimationResult` | Condition flat at ~3.3–3.6 across a 117× span (`EXP-000/metrics.json`) | **FIXED** | Test at the *operating* scale. NAC frames reach ~52 000 lines, so 60 000 px is normal, not an edge case. |
| **E-003** | PHASE-0 | Zero-filled invalid warp regions are indistinguishable from real terrain | **HIGH** | Conventional zero/edge fill is the library default | Domain reasoning during design, then pinned by test | `warp()` defaults to `cval = np.nan` plus an explicit boolean validity mask | `test_outside_source_is_marked_invalid_not_black` | **FIXED** | **Lunar shadow is genuinely near-zero.** A generic CV default becomes a domain hazard; defaults must be re-examined per domain. |
| **E-004** | PHASE-0 | Downsampling without prefilter aliases terrain texture into false structure | **MEDIUM** | Sampling below Nyquist | Constructed a near-Nyquist sinusoid and compared post-downsample variance with/without prefilter | `resize()` prefilters by default when shrinking, `σ = (1/s − 1)/2` | `test_downsampling_without_antialias_aliases` — aliased variance > 3× clean | **FIXED** | Aliasing can **manufacture correspondences that do not exist** (spec §41). A test that demonstrates the artefact justifies the default; a test that the code runs does not. |
| **E-005** | EXP-001 | Ground-truth generator wrong by **56.57 px** (= 40·√2) while RANSAC reported **99.6% inliers and 0.72 px fit RMSE** | **CRITICAL** | Reference rendered into a canvas padded by 40 px, then cropped, **without composing the offset into the returned GT transform** | **Only by comparison against ground truth.** No self-consistency check could have found it | `translation(pad, pad)` composed into `big_to_ref`, with an explanatory comment marking the term load-bearing | `test_ground_truth_is_exact_regression` — failure message names the 56.6 px signature | **FIXED** | A uniformly shifted match set is perfectly self-consistent. **Evaluation code needs the same scepticism as pipeline code** — this was ANALYSIS §B6 occurring inside our own harness. |
| **E-006** | EXP-001 → EXP-002 | LO-RANSAC ran local optimisation on **every** sufficient sample, not only on a new best — 198 large refits per 100 iterations | **MEDIUM** | Deviation from LO-RANSAC as published (Chum, Matas & Kittler 2003) | `estimate` call count (298) in the profile could not be explained by 100 minimal fits | `if count <= best_count: continue` before the LO block; `lo_max_points` caps the **refit sample only** | Refits 198 → 3; `test_lo_runs_only_on_a_new_best` (≤ 20, machine-independent) | **FIXED** | When re-implementing a published algorithm, re-check the trigger condition, not just the maths. Cap the refit, never the scoring — robustness must not be traded for speed. |
| **E-007** | EXP-001 → EXP-002 | `np.linalg.svd(full_matrices=True)` in the DLT built a **4230 × 4230 `U` (17.9 M elements)** and discarded it | **HIGH** | numpy's default; the DLT needs only the last row of `Vt` | `cProfile` by cumulative time — **51.55 s of 53.9 s inside `svd`** | `full_matrices=False`; one keyword | **498.86 ms → 1.01 ms (492.8×)**, singular values and `Vt` identical. `test_dlt_uses_the_economy_svd` asserts on shapes | **FIXED** | A 3-orders-of-magnitude cost hid inside an entirely idiomatic line. **Stage-level timing is what made it findable** — "RANSAC is slow" is not a diagnosis. EXP-001's own diagnosis of this symptom was incomplete and blamed only E-006. |
| **E-008** | EXP-001 | **Fit RMSE gives false confidence** — 11 of 17 failed cases reported < 1e-12 px while being 190–2400 px wrong | **CRITICAL** | At the minimal set the least-squares fit interpolates exactly, so the residual vanishes *by construction*; RANSAC selects for mutual consistency, keeping it near zero just above | Comparing fit RMSE against **true endpoint error** on a dense grid | Reporting rule: every RMSE quoted with its inlier count and labelled `fit` or `held-out`; headline claims use the worst applicable estimator | Confirmed by pre-declared negative control in EXP-002: **ROC AUC 0.495** on 192 unseen cases. `test_fit_rmse_collapses_when_inliers_approach_model_dof` | **FIXED (methodology)** | Fit RMSE is not merely circular — **in the failure regime it is inverted**, attaining its best value at the moment of total failure. An evaluation quoting it alone ranks catastrophes as its best results. |
| **E-009** | EXP-001 → EXP-002 | **Circular threshold claim** — "wrong ≤ 7 inliers, correct ≥ 8" derived and evaluated on the same 45 cases, at a value `classify_failure` already used | **HIGH** | No calibration/validation separation existed in EXP-001 | Independent validation on 192 cases from disjoint seeds and different terrain | Claim withdrawn; replaced by a **validated operating point** with a measured error rate | Validation: correct **min = 2** → separation claim `holds: false`. Rule `n_inliers < 8` → recall 1.000, FPR 0.0112 | **FIXED** | Distinguish *"the separation observed in this sample"* from *"the rule validated on unseen data"*. The first is a property of 45 cases; only the second is usable. |
| **E-010** | EXP-002 | **`split_consistency` AUC artefact** — apparent ROC AUC 0.994 was a proxy for low inlier count | **HIGH** | Estimator returns `inf` when there are too few inliers to evaluate; the sentinel aligned almost perfectly with the label | Counting non-finite values: **104/192 non-finite, 103 of them wrong**; of 88 finite cases, **0 wrong** | Automated `best_deployable_signal` selection **overridden** in the report; `n_inliers` reported as the best genuinely informative signal | Documented in `EXP-002/README.md` §3.5 and RL-020 | **FIXED (interpretation)** | A signal that collapses to a sentinel on half the data has its AUC measured on the sentinel. **Always check the finite subset.** The automated pick was wrong and the artefact was nearly reported as a result. |
| **E-011** | EXP-002 | **Held-out residual and split consistency are blind to coherent wrong solutions** | **HIGH** | Both measure **self-consistency**; a uniformly wrong set is perfectly self-consistent | Constructed adversarial cases with 300 correspondences, so counting cannot explain the outcome | Demoted to **degeneracy detection only** (ADR-0011); loop closure promoted to primary | Detection **0.200** (only the degenerate case). `lattice_shift`: 64 px wrong → held-out **0.468 px**, *lower* than a correct noisy set at 1.424 px | **FIXED (scope)** | ANALYSIS §F.1 named the wrong estimator as primary. The register records the reversal rather than quietly rebalancing a combined score. |
| **E-012** | EXP-002 | **Cycle-consistency blind spot** — cannot detect symmetric errors | **MEDIUM** | Forward +64 px and backward −64 px cancel exactly in the round trip | Constructed `symmetric_wrong` case; cycle error **0.000** on wrong cases vs **0.325** on the correct one — signal **inverted** | Retained with the blind spot documented and tested, not treated as a safety net | `test_cycle_consistency_is_blind_to_a_symmetric_error` | **DOCUMENTED** | The construction deliberately targets the blind spot; a real reverse pass is independent and would not cancel exactly. Defensible claim: **"cannot detect symmetric errors"**, not "useless". |
| **E-013** | EXP-001 → EXP-002 | **Unrealistic synthetic terrain** — p99 slope ≈ 78°; **89.55% of the surface steeper than the lunar angle of repose** | **HIGH** | `relief` conflated amplitude with spectral content; no slope target and no comparison to published lunar statistics | Measuring slope statistics and comparing with LOLA values (sources.md S7) | Explicit `target_slope_median_deg`, 4 named regimes; old terrain **retained** as `C_extreme_diagnostic` | A-regimes hit LOLA medians exactly (3.50 vs 3.5; 9.10 vs 9.1); `test_realistic_regimes_respect_the_angle_of_repose` | **FIXED** | "Realistic" is a claim and must be checkable against an authoritative source. Silently replacing the terrain would have made EXP-001 unreproducible **and hidden the change in conclusions**. |
| **E-014** | EXP-001 → EXP-002 | **Illumination cliff located too optimistically** — reported at Δaz 30–45°, actually 15–30° on realistic terrain | **HIGH** | Conclusion drawn from a single, unrealistically steep terrain whose strong shading gradients flattered the descriptor | Re-running the EXP-001 axes across 4 terrain regimes, 3 seeds | Cliff relocated; conclusions now reported **per regime**, drawn from realistic regimes only | A-regimes last fully successful at **15°**; B/C at 30° (`objective2_terrain.json`) | **FIXED** | A conclusion measured on one dataset is a conclusion about that dataset. **RootSIFT is worse on real-looking terrain than EXP-001 concluded.** |
| **E-015** | EXP-001 → EXP-002 | **"Scale survives to 4×" over-generalised** | **MEDIUM** | Scale tested only on texture-rich terrain | Regime sweep: realistic mare fails at 2× (success 0.33) and 4× (0.00) | Claim narrowed to textured terrain; mare promoted to priority case for EXP-003 | `objective2_terrain.json:exp001_subset_summary` | **FIXED** | Scale tolerance is a property of the descriptor **and the available texture** — 312 vs 22 119 kp/Mpx — not of the descriptor alone. |
| **E-016** | EXP-001 → EXP-002 | **RootSIFT advantage asserted from literature, not established here** | **LOW** | Adopted on citation; the comparison that was run had 3 points at one seed | Group G measurement, then EXP-002's ±0.59 px run-to-run variation | Retained (free, standard) but the **claim dropped** (ADR-0009) | 0.136 px difference at Δaz 0 is inside ±0.59 px → noise (RL-016) | **DOCUMENTED** | Spec §69: do not repeat a published claim we have not verified. Recorded as a **negative result** rather than quietly kept. |
| **E-017** | EXP-002 | **Mechanism mis-stated** — RL-010 said exact interpolation occurs at "3–4 correspondences" | **LOW** | Confusing model DOF (6) with minimal set size (3 pairs) | Direct test: 3 random pairs → fit RMSE < 1e-9; **4 random pairs → 27.85 px** | Wording corrected in ADR-0003 and RL-021; empirical finding unaffected | `test_exact_interpolation_only_at_the_minimal_set` | **FIXED** | The empirical observation was right and the stated mechanism was wrong. Correct the mechanism rather than the conclusion — RANSAC *selects* consistent points, which is why n slightly above the minimum stays near zero. |
| **E-018** | EXP-001 | **§B6 risk overstated** — repetitive terrain predicted as a primary danger | **LOW** | Reasoned from first principles without measurement | Ambiguity sweep: perfect results (0.006–0.016 px) at ambiguity ≤ 0.9 | Risk R5 probability lowered Med → Low-Med; `ambiguity` parameter added so the claim is testable | The failure needed *all* disambiguating context removed; comment retained in `synthetic_terrain.py` | **DOCUMENTED** | Ratio test + mutual NN is more robust to repetition than assumed. An adversarial scene that has to be built to order is weaker evidence than one that arises naturally. |
| **E-019** | REAL-DATA | **ODE spatial query silently ignored longitude** — `minlon`/`maxlon` are not the parameter names, so the service returned `Status: Success` and filtered on latitude only | **HIGH** | Unknown query parameters are accepted and dropped without warning; the correct names are `westernlon`/`easternlon` | Checked returned product centres against the box requested: a 1 deg longitude request returned centres spanning **97.9 deg to 296.6 deg** — the whole Moon | Correct parameter names, plus `_verify_box()` which raises if returned centres span far more than the request | 3 regions now return correctly-bounded products; 117 candidate pairs found where 0 were before | **FIXED** | **A "successful" query is not a correct query.** An API that drops unknown parameters silently turns a typo into plausible, well-formed, wrong data. Always verify the response against what was asked for. |
| **E-020** | REAL-DATA | **PDS4 NAC labels carry no illumination geometry**, contradicting this project's own module docstring which said azimuth should be read from them | **MEDIUM** | Assumed PDS labels carry Sun angles; they do not for these products, and ODE exposes incidence/emission/phase but no sub-solar **azimuth** | Grepped 6 downloaded labels for `incidence`/`sub_solar`/`emission`: **0 hits** in all of them | Docstring corrected to state the gap; pair metadata now carries `azimuth_status` recording that azimuth is unavailable | Pairs are labelled **illumination-varied, NOT azimuth-controlled** everywhere they appear | **DOCUMENTED** | **Azimuth is the project's dominant axis (EXP-001) and it is the one angle the archive does not publish.** Deriving it needs a solar ephemeris. Until then no real-data azimuth claim is possible. |
| **E-021** | DEMO | **Loop closure reported 0.000 px on a registration 64 px wrong** — the demo said VERIFIED / high confidence | **CRITICAL** | The loop's closing edge was derived algebraically as `(t_bc @ tf_est).inverse()` instead of being estimated from images, so the error appeared in two edges and cancelled exactly | The adversarial scenario returned `VERIFIED` with true error 63.99 px — caught because the demo reports synthetic ground truth alongside the verdict | All three loop edges now estimated **independently** by running the pipeline on three real image pairs | Adversarial case: loop **63.998 px → REJECTED**; control case: loop **0.008 px → VERIFIED**. `test_loop_closure_edges_must_be_independent_estimates` | **FIXED** | **This is E-012's symmetric-error blind spot, reintroduced by construction in the one place the project claims to be strongest.** A consistency check built from the quantity it is checking verifies nothing. Independence is the whole property. |
| **E-022** | REAL-DATA | **A `Product_Browse` label was accepted for 4 of 6 products**, including **both** halves of the best `mare_serenitatis` pair. Browse labels describe a JPEG pyramid and carry no `File_Area_Observational`, no dimensions, no offset and no data type, so decoding was impossible and the reason was invisible | **HIGH** | `_parse_product` selected label files by **extension** and assigned unconditionally (`elif url.endswith(('.XML','.LBL')): lbl_url = url`), so the **last** `.xml` in ODE's list won — and ODE lists `Type=Browse` *after* `Type=Product`. Products carrying a browse pyramid (7 files) got the wrong label; those without one (5 files) were right **by accident** | Compared the root element of each stored label against what decoding needs: **4 × `Product_Browse` (~2 KB), 2 × `Product_Observational` (~13 KB)**; grep for image-structure elements gave **0 hits** in the browse labels and 7 in the observational ones | Select files by ODE's explicit **`Type`** field — a documented, ODE-specific mapping — and keep the browse URL in `NacProduct.browse_label_url` rather than discarding it. `observational_label_url()` **refuses to synthesise** a URL (the `DATA/<phase>/` segment is not recoverable from the browse path). Independently, `fetch_label()` validates the root element **and** the image structure of whatever actually arrives, and writes to disk only after it validates | Both `mare_serenitatis` observational labels re-fetched live and validated (`Product_Observational`, structure parsed, `file_size` identity exact). `test_ode_product_label_wins_over_browse_label_listed_after_it`, `test_ode_label_selection_is_independent_of_file_order`, `test_untyped_records_never_select_a_browse_path_label`, `test_observational_label_url_refuses_to_synthesise_from_a_browse_url`, `test_browse_label_is_rejected_not_parsed` | **FIXED** | **When a service classifies its own data, read the classification.** Inferring type from a filename discards information the API already supplied, and the failure is silent because both files are valid PDS4 XML at the same URL shape. This is **E-019's** lesson in a new place: the response was successful and wrong. |
| **E-023** | REAL-DATA | **The decode-correctness statistic collapsed onto the `-32768` sentinel border** — byte-order and alignment evidence reported **0.9919 for two different frames**, identical to four decimal places, with a vertical correlation of exactly **1.0000** | **HIGH** | `byte_order_evidence` / `byte_alignment_evidence` computed lag-1 autocorrelation with `mask_special_constants=False` over the full 5064-sample width. A whole-line NAC read carries wide constant `missing_constant` border columns whose variance (~3664 DN) dwarfs the scene's (~19.6 DN); two constant blocks separated by real data give a correlation near 1 regardless of image content. **The statistic measured the border, not the image** | The impossible coincidence itself: two different frames cannot share a statistic to four decimals. Confirmed by column medians (`-32768` for the first and last 8 columns, ~29 in the centre) and by an exactly-1.0000 vertical correlation | Both evidence functions now **mask sentinels** and apply the **same sample window** as the tile under test | `test_sentinel_border_does_not_dominate_the_autocorrelation`: two framed scenes of very different contrast differ by < 1e-3 unmasked (indistinguishable) and by > 1e-3 masked. On the real tiles the statistic now equals the plain autocorrelation exactly (0.9590 / 0.9415 / 0.9759 / 0.3553) | **FIXED** | **This is E-010 recurring.** A statistic that collapses onto a sentinel measures the sentinel, and the tell was identical both times — an implausibly round, implausibly *identical* number. Always check what fraction of the data is sentinel before trusting any summary computed over it. |
| **E-024** | REAL-DATA | **A 0.0022 margin was reported as proof that working code had the byte order wrong**, sending the reader to debug a correct decoder | **MEDIUM** | The decode verdict used a bare `>` comparison between the declared and the byte-swapped decode. On a noise-dominated tile every decode is equally incoherent, so the test has **no discriminating power** there and the sign of a ~1e-4 difference is arbitrary | On the terminator tile the declared decode scored 0.3553 against 0.3531 swapped — a margin of **0.0022** — and the check announced a byte-order defect. Reproduced deliberately on a synthetic pure-noise tile, where the swapped decode "won" by 0.0002 | Introduced `DECODE_EVIDENCE_MARGIN = 0.10`. Below it the verdict is **`inconclusive`**, stated as such with both margins reported. The low-autocorrelation failure is now split: with a `consistent` **or** `inconclusive` decode it is classified **`data quality (low SNR)`**, not a decoding defect | `test_check_tile_blames_data_quality_not_the_decode_for_a_noisy_scene`, `test_thin_decode_margins_are_never_reported_as_a_decode_defect`, and `test_check_tile_fails_a_byte_swapped_tile_and_blames_the_decode` (a genuine 0.256 margin is still caught) | **FIXED** | A discriminator needs a stated **margin** and a stated **domain of validity**. "Inconclusive" is a result; reporting it as a defect wastes the next reader's time on correct code. What actually justifies trusting the terminator tile's decode is that the *same code path* scores 0.9759 on its partner frame — argued from elsewhere, exactly as the check now says. |
| **E-025** | REAL-DATA | **Acquiring the same product at a second line offset silently overwrote the first tile**, so a four-way H1/H2 comparison returned **byte-identical results in all four rows** (9740/13411 keypoints, 43 putative, 5 inliers, fit RMSE 8.135e-13) | **HIGH** | Tiles were written to `{pdsid}.tile.npy`, which encodes the product but **not the tile position**. The second acquisition overwrote the first and both manifests pointed at one file. This also violates **integrity rule 4** — never overwrite a superseded artefact | The identical numbers themselves: four genuinely different tile pairs cannot produce one result. Traced to the filename by comparing the manifests' `tile_npy` paths | Filenames now encode position: `{pdsid}.{hypothesis}.l{line0}.tile.npy`. Both hypotheses re-acquired. The comparison script now **asserts the four tiles have four distinct SHA-256 values before interpreting any result** | The re-run produced four genuinely different rows (**3, 4, 4, 5** inliers). The H1 re-fetch reproduced its original SHA-256 **byte for byte**, which is independent evidence that the byte-range fetch is deterministic. Distinctness assertion recorded in `experiments/REAL-DATA/hypothesis_grid.json` | **FIXED** | **Identical results across supposedly different conditions are a bug report, not a finding.** The instinct to explain the coincidence scientifically — "the direction hypothesis doesn't matter" — would have produced a plausible, published, wrong conclusion. Encode every varying parameter in the artefact's name. |
| **E-026** | REAL-DATA | **Maximising Δincidence selects the terminator, where one frame is unusable.** The selector's top `mare_serenitatis` pick was 89.96° vs 24.64°; an incidence of 89.96° puts the Sun 0.04° above the horizon | **MEDIUM** | A difference-maximising criterion with **no upper bound on either operand** necessarily selects the extreme of the range, and the extreme of incidence is the terminator. `find_illumination_pairs()` has an incidence-difference *floor* and no *ceiling* | Sanity checks on the decoded tile: DN median **29 ± 19.6** (I/F ≈ 0.00088) and lag-1 autocorrelation **0.3553**, against 1582 ± 133.6 and 0.9759 for its partner — a **55× brightness difference**. Only 2 515 keypoints detected against 9 706, and the pair yielded **5 putative matches** | **Not yet fixed in code.** Recorded as **D-029**: pair selection needs an incidence **ceiling** as well as a difference floor. A survey with both incidences ≤ 75° gave **32 usable pairs**; the best (`nac.m1271742202lc` × `nac.m1335207975rc`, Δinc 39.8°, IoU **0.516**, resolution ratio 1.016) became the primary case | The tile decodes correctly (`file_size` identity exact, SHA-256 reproducible) and fails the sanity check on **data quality**, not on ingestion — which is the distinction E-024 exists to preserve. `experiments/REAL-DATA/tile_sanity_terminator.json` | **DOCUMENTED** | An optimisation criterion is a specification of what you will get. **"Most illumination difference" and "most *informative* illumination difference" are different objectives**, and the archive contains enough range to make the difference fatal. |
| **E-027** | REAL-DATA-02 | **A `NORTH_AZIMUTH` cross-check was designed, implemented, and has no discriminating power.** Run on the four real frames it reports *agrees* for two and *disagrees* for two, which reads as the archive contradicting itself and would send the next reader to debug a correct corner assignment | **MEDIUM** | The check was designed from the column's *name* and its angle convention without reading its stated **domain of validity**. `INDEX.LBL` says the angle is *"relative to the RDR products"* -- the map-projected derivative, north-up by construction -- so it is ~270 deg for every LROC CDR regardless of line order | `NORTH_AZIMUTH` = **268.59 / 274.77 / 267.72 / 272.94** -- a **5.70 deg** spread between the two line-direction groups, while the frames split **2-2** on line direction | `north_azimuth_discriminating_power()` evaluates the column across a *set* and reports `has_power = False` when the frames split but the azimuths do not (< 15 deg between group means). The per-frame verdict is still computed, recorded, and **explicitly not used**. Nothing about the corner naming was changed | `test_a_constant_azimuth_across_a_split_set_has_no_power` (the real four values), `test_a_set_whose_azimuths_track_the_line_direction_has_power`, `test_a_set_with_only_one_line_direction_cannot_show_anything` | **FIXED** | **This is E-024 recurring**, and recognising it is why it cost an hour rather than a day. E-024's lesson was that a discriminator needs a stated margin **and** a stated domain of validity; here the domain of validity was written in the archive label and not read. **A disagreement reported by a test with no power is worse than no test -- it manufactures doubt about correct data.** |
| **E-028** | REAL-DATA-02 *(defect in REAL-DATA-01's acquisition)* | **The line-direction hypothesis was forced to be global, and it is per-frame.** `acquire_real_pair.py` applies one `--hypothesis` to both frames. Both `usable` frames are H2; the pair was acquired at H1. **Its two tiles landed 22.75 km apart**, and that pair produced REAL-DATA-01's headline 3-inlier result | **HIGH** | The line readout direction relative to the ground track is set by **spacecraft attitude**, not by the camera or the pipeline. LRO yaw-flips, and these products span 2016-2020 across both attitudes. The script's docstring reasoned *"same camera, same processing pipeline, so the convention is the same for both"* -- true and irrelevant. The archive publishes the answer and it was not consulted, because REAL-DATA-01 did not know the index table existed | `LRO_FLIGHT_DIRECTION` = **-X** for both usable frames (UPPER at MIN latitude) and **+X** for both terminator frames (UPPER at MAX). Independently, corner-implied pixel scale agrees with `SCALED_PIXEL_HEIGHT`/`WIDTH` to within **1.2 % on all 8 comparisons** only under this assignment. Overlap consequence: `overlap_verification.json` case `usable_H1`, intersection **0.0000 km2** | **Not fixed in `acquire_real_pair.py`, deliberately** -- fixing it means re-acquiring tiles and re-running registration, which is REAL-DATA-03. This stage delivers the *means*: `FrameCorners.pixel_at()` inverts the ground map, and REAL-DATA-02 section 8.4 gives the corrected `(line0, sample0)` for both pairs, projecting to **97.1 %** overlap. Recorded as **D-033** | `test_pixel_at_inverts_lonlat_at_exactly` (7 pixels incl. all four corners), `test_a_window_centred_by_pixel_at_lands_on_the_requested_ground_point`, `test_along_track_mirroring_moves_a_tile_by_the_length_of_the_frame` | **DOCUMENTED -- fix deferred to REAL-DATA-03 (D-033)** | **An assumption justified by a plausible physical argument is still an assumption, and this one had a published answer.** The docstring's reasoning was careful, explicit and wrong, and its carefulness is what made it survive -- it reads like a derivation. The tell was already on the page: REAL-DATA-01 section 17.3, *"the line-direction ambiguity is unresolved; the data does not decide it."* **"The data does not decide it" is a statement about the data one has looked at.** |
| **E-029** | REAL-DATA-02 *(defect in REAL-DATA-01's acquisition)* | **Cross-track tile position was never matched between frames.** Each tile takes its own frame's centre 2048 samples, on the stated reasoning that *"the two frames' longitude ranges overlap in their middles"*. They are offset instead | **MEDIUM** | Along-track position was derived from the footprint; cross-track was **assumed**. A NAC frame is 5064 samples ~ 4.6 km wide and the tiles are 2048 samples ~ 1.8 km -- never enough margin for a centre-of-frame assumption to absorb a 1.7 km offset | Centre longitudes differ by ~0.04 deg (usable) and ~0.06 deg (terminator) = **1.1 km and 1.7 km**, against a **1.8 km** tile. Measured consequence: `usable_H2` reaches **68.6 %** where the same frames allow **97.1 %**, and `terminator_H1` is capped at **28.3 %** -- which is what leaves it **OVERLAP_UNKNOWN** | Not fixed in the acquisition script, same reason as E-028. `pixel_at()` plus the projected windows supply the correction; **D-033** carries it | Projection recomputed through the same tested path (`recommended_windows` in `scripts/verify_tile_overlap.py`), giving **97.1 %** and **82.5 %** | **DOCUMENTED -- fix deferred to REAL-DATA-03 (D-033)** | **A selection that constrains one axis and assumes the other has not been verified -- it has been half-verified, which reads the same in a manifest.** The manifest recorded `overlap_latitude_band_deg` and nothing about longitude, and that asymmetry was visible on the page the whole time. |
| **E-030** | REAL-DATA-03 | **The raw-tile byte cache was keyed by product, not by window.** `check_real_tiles.py` cached tile bytes as `{pdsid}.tile.raw`, so running it on the REAL-DATA-03 tiles loaded the REAL-DATA-01 bytes for the same products and computed the byte-order and alignment evidence from a window **22.75 km away** from the tile under test | **HIGH** | An artefact name that omits a varying parameter. **This is E-025 recurring in a second script** -- E-025 was `{pdsid}.tile.npy` losing the line offset, and its lesson was *"encode every varying parameter in the artefact's name"*. The `.raw` sidecar in a different file was never audited against it. A **second defect on top**: the mismatch WAS caught by the SHA check and reported as *"the range fetch is not reproducible"* -- false, and it sends the reader to debug a correct fetch (E-024's lesson in a new place) | The failed run is preserved at `experiments/REAL-DATA/tile_sanity_usable_geo_preE030.json`: it reports SHA-256 `56236a0fe23f3484` (REAL-DATA-01's tile at line 30126) for a tile whose manifest records `de96b5e885dc9871` at line 17955, and computed the decode evidence as **0.9612** where the correct bytes give **0.9655** | Sidecar renamed `{pdsid}.l{line0}s{sample0}n{n_lines}.tile.raw`. Every cache hit is hash-verified against the manifest **before** use; a mismatch is not an error but the wrong entry, so it is discarded with a printed note and re-fetched. The pre-E-030 name stays readable -- REAL-DATA-01 wrote 166 MB of sidecars -- but **only when its hash proves it holds the right bytes**. A fetch refuses to overwrite an existing sidecar, and the failure message now states that reaching it means the ARCHIVE returned different bytes | Six tests in `tests/test_real_tile_cache.py`, driven through an in-memory reader with no network: two-window name separation, hash-verified hit, the exact E-030 scenario (stale legacy file discarded AND the re-fetch happens), legacy hit honoured on its hash, overwrite refusal, manifest/label range disagreement. The re-run passes all three tiles | **FIXED** | **A lesson recorded in the ledger only protects the file it was learned in.** E-025's fix was applied to the acquisition script and its rule written down; the same rule was violated in the sanity script one directory away and nothing checked. When a failure mode is named in the ledger, **grep for its shape across the repository**, not just the line that caused it. |
| **E-031** | REAL-DATA-03 | **A diagnostic figure crashed and took a set of passing checks with it.** `check_real_tiles.py` laid its figures out as `plt.subplots(2, 3)` because it had only ever been run on pairs; on the three-tile loop manifest it raised `IndexError: index 2 is out of bounds for axis 0 with size 2` -- **after** all three sanity checks had run and passed, and **before** the JSON report was written | **MEDIUM** | Two independent things: a hard-coded row count, and an ordering in which the measurement was persisted *after* the presentation | The traceback, with `PASSED: True` printed for all three tiles immediately above it | The grid is sized from the number of tiles (`squeeze=False`), and **the report is written before any figure is drawn**. The side-by-side caption now reflects whether the windows were geometry-driven rather than always claiming the footprint-latitude approximation | The triplet run writes `tile_sanity_real_triplet_geo.json` with all three tiles passing, then both figures | **FIXED** | **Persist the measurement before rendering it.** A figure is a convenience; a measurement is the result. Any ordering in which a presentation failure can destroy a completed measurement is wrong regardless of how unlikely the failure looks. |
| **E-032** | REAL-DATA-04 *(finding about REAL-DATA-02's rule, and about a claim in REAL-DATA-03)* | **The flight-direction rule for which latitude image line 0 sits at is not a rule.** REAL-DATA-02 recorded, and REAL-DATA-03 §10 called a *"fifth independent confirmation"* of, `LRO_FLIGHT_DIRECTION = -X` -> line 0 at **minimum** latitude, `+X` -> **maximum**. Fetching authoritative corners for five more frames produced **two counterexamples**: `nac.m1343417565rc` is `-X` with line 0 at maximum latitude, and `nac.m124423514lc` is `+X` with line 0 at minimum | **MEDIUM** | A regularity that held on five frames was written up in the language of a rule. All five had `NORTH_AZIMUTH` in 267.7-276.8 deg, so the column quoted as its cross-check **had no discriminating power over the line direction in that sample** and could not have contradicted anything, however many frames were added | `[MEASURED]` `data/manifests/real_frame_d_index_geometry.json`, `real_frame_d_candidates_index_geometry.json`. Over the **10** frames with authoritative named corners, the `(along-track, cross-track)` signature splits **(-1,-1) x4, (1,-1) x4, (-1,+1) x1, (+1,+1) x1**. `LRO_FLIGHT_DIRECTION` predicts the along-track sense **8 / 10**: `+X` gives {-1: 4, +1: 1}, `-X` gives {+1: 4, -1: 1}. `ORBIT_NODE` is `A` for all ten and predicts nothing. What `NORTH_AZIMUTH` **does** separate, 10 / 10, is the **cross-track** sense: 86.68-88.55 deg for longitude increasing with sample (n=2), 267.22-276.76 deg for decreasing (n=8) -- a different axis from the one it was quoted for | **No pipeline change was needed, and that is the point.** `corners_from_index_geometry()` has always read each product's own PDS4 `disp:Display_Direction` and refuses a frame whose label disagrees; it never consulted the rule, which lived only in prose. The rule is **demoted to an observed 8/10 regularity with two counterexamples**, and the `NORTH_AZIMUTH` cross-check is restated as being about the **sample** axis | `orientation_signature()` in `scripts/screen_frame_d.py`, applied as REAL-DATA-04 hard filter 1c to every candidate from **named** archive columns; it rejected that stage's top-ranked candidate. Tests in `tests/test_screen_frame_d.py` | **DOCUMENTED -- prose corrected, no code defect** | **A cross-check with no discriminating power in the sample it was validated on is not a cross-check, and counting it as a confirmation makes a rule look stronger the more uniform the sample gets.** `footprint.py` already ships `north_azimuth_discriminating_power()` for exactly this, and its answer for those five frames would have been *no power*. The check existed; the confirmations were counted without it. **Before calling the n-th instance a confirmation, ask what result would have falsified it.** || **E-033** | **Pre-freeze audit (2026-08-29)**, finding about artefacts written by REAL-DATA-02/03/04 | **Five judge-facing artefacts record the `stage` of the script's DEFAULT, not of the run that produced them.** `experiments/REAL-DATA-03/overlap_triplet.json` and `overlap_usable_geo.json` record `REAL-DATA-02`; `experiments/REAL-DATA-04/overlap_real_data_04.json` records `REAL-DATA-02`; `experiments/REAL-DATA-04/transform_vs_geometry_real_data_04.json` and `data/manifests/real_quad_d_geo_manifest.json` record `REAL-DATA-03`. | **LOW scientifically, HIGH for credibility** | A stage id hard-coded as a literal in the report payload of a script that was later reused by a subsequent stage. `register_real_triplet.py` had already been given a `--stage` argument for exactly this reason in REAL-DATA-04; `verify_tile_overlap.py`, `check_transform_against_geometry.py` and `acquire_real_pair.py` had not, and nothing compared the field against the directory it was written into | `[MEASURED]` Enumerated mechanically across every tracked `experiments/**/*.json` and `data/manifests/*.json`: exactly five files carry a `stage` that disagrees with their location. **Every scientific quantity in all five was independently recomputed during the same audit and reproduces**: the six recorded overlap intersections to within 0.07 % and the six transform-vs-geometry disagreements to within 2.0 %, using a different projection (gnomonic), a different area algorithm (Monte-Carlo point-in-polygon) and a different intersection method than `footprint.py` uses. The defect is a label, and only a label | **The artefacts are NOT edited.** Integrity rule 3: a recorded file is not rewritten, and re-running to correct one cosmetic field would replace evidence that has been quoted in three stage reports. The fix is in the producing scripts -- `verify_tile_overlap.py` and `check_transform_against_geometry.py` now derive `stage` from `--outdir` with an explicit `--stage` override, so an artefact cannot record a stage other than the directory it is written into | `test_recorded_artefact_stage_labels_are_either_correct_or_known` enumerates the mismatches and fails if a **new** one appears, while accepting exactly these five as recorded history | **DOCUMENTED -- scripts fixed, artefacts frozen** | **A field nothing compares against anything is not provenance, it is decoration.** The pipeline's real provenance -- SHA-256, byte ranges, windows, corner sources -- is cross-checked at every step and held. The one field with no consumer drifted immediately and stayed wrong across three stages. **If an artefact records a fact, something must fail when that fact is wrong**; otherwise do not record it. This is E-027's lesson (*a cross-check with no discriminating power is not a cross-check*) applied to a self-describing field rather than to a comparison. |
| **E-034** | **EXP-010 (2026-09-04)**, finding about the baseline configuration used by REAL-DATA-03/04 and every recorded edge | **The default affine model absorbs structured keypoint-localisation error and reports a zero residual while doing it.** On frame D's recorded tile, a pure integer self-shift of (7, -5) px is recovered by a translation model to **0.002 px**; the affine model recovers it to **0.989 px median, 2.306 px maximum** endpoint error over the tile, with **11849 inliers** and an inlier residual median of **0.000 px**. The spurious linear part is a shear of 3.1e-3 (3 px across 1000 px). Frame A shows the same effect at 0.23 px; frames B and C do not | **MEDIUM** -- an accuracy defect of up to ~2 px on a pair the rule calls a success; it changes no pass/fail outcome and no recorded conclusion | An over-parametrised global model (affine, the ADR-0001 default carried from the SAR-optical benchmark) fitted to localisation errors that are spatially *correlated* on a low-contrast, high-Sun tile (D at 18 deg incidence, percentile-stretched noise). Least squares distributes the correlated error into the linear terms; the fit residual, being self-consistency, cannot see it (E-008 family) | `[MEASURED]` Found because EXP-010's v1 gate refined the B1 estimate of an integer shift and returned a 0.3 px median shift on frame D only; diagnosed by fitting translation and affine models to the same self-warp and comparing dense endpoint error (session log 2026-09-04). Recorded in `experiments/EXP-010/exp010_results.json` (v1 gate rows) | **No recorded artefact is changed.** Two fixes, both already in the architecture: (1) local sub-pixel refinement corrects it point by point (EXP-010 S1: 0.003 px after refinement on the same tile); (2) model selection by held-out predictive error before a model is accepted, to be pre-registered as its own stage | pending -- a model-selection test on self-warps of all four recorded tiles, translation vs similarity vs affine vs projective, is the next step | **OPEN -- documented, fix designed** | **A model with more freedom than the geometry needs turns localisation bias into geometric error, and the fit residual is silent about it.** Choose the model by evidence the fit did not use (held-out points, a simpler model's dense disagreement), and refine locally so a global model's bias cannot become the answer. |

---

## Patterns worth carrying forward

1. **Five of the most severe entries (E-001, E-005, E-008, E-009, E-014) were invisible to every self-reported metric.** Each required an external reference — physical resampling, ground truth, disjoint validation seeds, or published lunar statistics. Self-consistency is not evidence.
2. **Two entries (E-005, E-010) were errors in the *evaluation* code, not the pipeline.** Harness bugs are more dangerous than pipeline bugs because they corrupt every conclusion at once and tend to flatter.
3. **E-007 was hidden inside idiomatic code.** Stage-level timing, not code review, found it.
4. **E-013 → E-014 → E-015 are one causal chain**: an unvalidated dataset assumption propagated into two wrong conclusions. Validate the *generator* before trusting anything measured on it.
5. **Six entries are over-claims rather than defects** (E-008, E-009, E-014, E-015, E-016, E-018). The dominant risk in this project is not broken code; it is **believing a result more than the evidence supports**.
6. **The same failure modes recur in new code, and recognising the recurrence is what makes them cheap to find.** E-023 *is* E-010 — a statistic collapsing onto a sentinel — occurring in measurement code written eight months later. E-022 *is* E-019 — a service returning a successful, well-formed, wrong response. E-021 was itself E-012 reintroduced. **A ledger is only useful if the patterns in it are read before the next module is written**, not after.
7. **An implausible coincidence is a bug report, not a finding** (E-025). Four "different" conditions returning byte-identical numbers had an available scientific explanation, and it was wrong. Assert that inputs differ *before* interpreting outputs that depend on the difference.
8. **A uniformly negative result across conditions can be evidence about the sampling rather than about the variable** (E-028). REAL-DATA-01 tested four line-direction combinations, saw 3/4/4/5 inliers, and concluded the direction hypothesis was not the explanation. Independent geometry later showed **three of those four tile pairs were 11–23 km apart and could not have succeeded**. The inference *"all conditions failed, therefore this variable does not matter"* is valid only if every condition was otherwise capable of succeeding. This is E-025's mirror image: there, identical results across supposedly different conditions were a bug; here, uniformly negative results across genuinely different conditions were read as a finding.
9. **"The data does not decide it" is a statement about the data one has looked at** (E-028). The line direction was correctly judged unrecoverable from the ODE footprint polygon — and the archive index table one directory above the data names its corners outright, along with `LRO_FLIGHT_DIRECTION`. Before concluding that evidence is absent, establish where that evidence would live if it existed.
10. **A lesson recorded in the ledger only protects the file it was learned in** (E-030). E-025's rule -- *encode every varying parameter in the artefact's name* -- was written down, applied to the acquisition script, and then violated in the sanity script one directory away, where it silently fed one tile's bytes into another tile's decode evidence. **When a failure mode is entered here, grep for its shape across the repository**, not just the line that caused it. The three recurrences so far (E-010 -> E-023, E-019 -> E-022, E-025 -> E-030) were each found by recognising the shape, not by a test.
11. **Persist the measurement before rendering it** (E-031). A figure that cannot draw must not be able to destroy checks that have already passed. Ordering the write after the plot is a latent data-loss bug in every analysis script that does it.
12. **If an artefact records a fact, something must fail when that fact is wrong** (E-033). A `stage` field that no test compared against the directory it was written into drifted on its first reuse and stayed wrong through three stages, in files a judge is invited to open. Self-description without verification is decoration. **Either check a recorded field or do not record it.**
